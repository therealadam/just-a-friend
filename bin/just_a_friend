#!/usr/bin/env ruby

require 'just_a_friend'

username = 'therealadam'
friends = Twitter.friends(:params => {:screen_name => username})
friends.map { |f| puts "#{f['name']} - #{f['screen_name']} - #{Delicious.user_bookmarks(:username => f['screen_name'])}" }
